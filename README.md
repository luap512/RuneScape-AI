# RuneScape-AI
This bot answers OSRS questions using the real wiki, not a language model's memory. It retrieves the relevant wiki passages first, then generates an answer grounded in that text and cites the source page. Built with Java, Spring Boot, PostgreSQL with pgvector, and React. Ingests pages through the MediaWiki API.
